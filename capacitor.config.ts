import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.threesouls.telugu.orderbooks',
  appName: 'OrderBooks',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
